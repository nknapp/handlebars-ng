---
import prettier from "prettier";

import { Code } from "astro/components";

export interface Props {
  ast: Record<never, unknown>;
}

const { ast } = Astro.props;
const prettyAst = await prettier.format(JSON.stringify(ast), { parser: "json" });
---

<Code code={prettyAst} lang={"json"} />
